# DESIGN.md — Brief 智能工作台

## 气质与意象
面向车企市场/传播岗的专业合规生产工具。意象锚点：清晨写字楼里法务与品牌共用的一张白色校对台——左手边是还没签字的 Brief 打印稿，右手边是摊开的《广告法》红笔批注本。整体克制、可信、高密度信息但不拥挤；像 Linear / 飞书文档 / Notion 企业版，不像营销落地页，更不像 C 端 AI 玩具。

## 视觉策略
- 桌面端优先，左右分栏：左侧为导航/列表面板（窄、固定、浅灰底），右侧为工作区（白底卡片）。
- 不使用插画与大面积渐变；仅用细分隔线、轻投影（shadow-sm 级别）、色块标签建立层级。
- 违禁词高亮是页面里唯一允许出现的高饱和色彩，且严格按风险等级编码，形成全局统一的"风险语言"。

## 配色方案
- 品牌主色：深石墨蓝 `#1F4E79`（正式、企业感，区别于泛滥的亮科技蓝），Hover `#183F63`。
- 背景：App 底 `#F5F6F8`，工作区卡片 `#FFFFFF`，侧边栏 `#FAFBFC`。
- 文字：主文字 `#1F2329`，次要文字 `#646A73`，占位/弱提示 `#8F959E`。
- 边框：`#E5E6EB`，分隔 `#EFF0F1`。
- 风险三色（仅用于违禁词体系，禁止挪用）：
  - 高风险 high：`#D93026`（朱红，背景 `#FDECEC`，下划线 2px 实线波浪）
  - 中风险 medium：`#D97A00`（琥珀橙，背景 `#FDF3E3`）
  - 低风险 low：`#B8860B` 降饱和为 `#A8801C`（麦黄，背景 `#FAF4E0`）
- 成功/安全：`#1A8754`（仅"未发现风险/检测完成"状态点使用）。

## 字体排版
- 字体族：`"PingFang SC", "Microsoft YaHei", "Noto Sans SC", system-ui, sans-serif`；数字与英文可用 `Inter`（通过 fonts.googleapis.cn 引入，含 Noto Sans SC 回退）。
- 标题 16px/600，正文 14px/400（行高 1.7），辅助文字 12px。
- 排版节奏：模块卡片之间 16px，卡片内 padding 20px；标签与输入控件上下 8px 节奏。
- Brief 模块标题用"编号 + 中文名"的组合（如 `03 核心卖点`），不再显示英文小标签，成品预览、编辑卡片与导出的 Word 三处保持一致。

## 动效与交互
- 性格：轻、快、确定。过渡 150–200ms，缓动 `cubic-bezier(0.4, 0, 0.2, 1)`。
- AI 流式生成：模块按到达顺序逐块"淡入上移 8px → 落位"，正文打字机式追加；生成中骨架块呼吸（opacity 0.5↔1，1.6s）。
- 违禁词命中：文字实时出现波浪下划线（纯 CSS text-decoration: wavy underline），右侧风险列表项点击后正文对应高亮块短暂闪烁强调框。
- 替换建议：点击后本地原地替换，高亮即时消失并 Toast 确认；不做弹跳等夸张动效。
- 不做：视差、渐变光球、全屏 Loading、emoji 表情。

## 设计禁忌
- 禁止亮紫蓝渐变、霓虹发光、毛玻璃大背景。
- 禁止圆角超过 10px 的"奶油风"卡片与彩色阴影。
- 禁止把风险三色用于普通按钮、链接等非风险语义处。
- 禁止在正文工作区使用大字号（>18px）营销标题。
- 禁止为"AI 感"加机器人图标/闪光特效；AI 入口仅用统一主色按钮 + 小字说明。

## 布局与响应式
- 桌面端（≥1200px）为核心：工作台 320px 左栏（Brief 列表）+ 弹性主区；合规/词库页为 224px 侧边导航 + 主内容。
- 1024–1200px 左栏收窄至 260px；<1024px 左栏退化为顶部抽屉（P0 保证可用，不做精细化移动端适配）。
