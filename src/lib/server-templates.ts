/**
 * 服务端 AI 生成用的模板规格（与 client 的 templates.ts 保持同一套 key）。
 * 只放「给模型看的指令」，避免 route.ts 直接依赖带 uid 等 client 工具的模块。
 */

export type GenTemplateId = 'general' | 'koc' | 'joey';

export interface GenModuleSpec {
  key: string;
  title: string;
  /** 该模块的生成指令 */
  guide: string;
  kind?: 'rich' | 'note';
  /** 选填模块：仅当素材确有对应内容时才输出 */
  optional?: boolean;
}

/**
 * 如实摘录模式（KOC 模板核心约束）：
 * 只搬运、不创作。把素材里已有的信息按模块如实填入，原话粘贴，
 * 不补充、不脑补、不润色、不改写、不归纳素材没有的结论。
 */
const FIDELITY =
  '本任务是「如实摘录」，不是「创作撰写」。严格只使用原始素材中已经出现的信息，按模块把对应内容原话填入：\n' +
  '- 只搬运、不创作：不得补充素材里没有的事实、数据、卖点、标题、观点、人群或结论；不得脑补、推测或借常识扩展。\n' +
  '- 原话粘贴：尽量保留素材原文措辞与数据，不做润色、改写、同义替换或"更专业"的重新表达；仅允许删除与该模块无关的内容、调整顺序以对号入座，以及为符合下方表格/列表格式做最小切分。\n' +
  '- 完整不遗漏：素材中属于该模块的信息要完整呈现，不得因"不够书面"而省略或简化。\n' +
  '- 必填模块若素材确实没有对应信息，在该模块正文只写「素材未提供」四个字，绝不编造；选填模块若素材无对应内容，则整个模块（含模块行）都不要输出。\n' +
  '- 不改变合规事实：即使素材原文含有绝对化用语或风险表述，也照实摘录、不要替用户改写（系统会另行做违禁词检测）；但绝不主动新增素材中不存在的承诺、排名或数据。';

const COMPLIANCE =
  '严禁《广告法》绝对化用语（如“最、第一、首选、首选车型、顶级、国家级、销量冠军、唯一、领导者”等，除非素材明确给出可引用的官方排名并标注统计口径与时间段；示例标题中也不得出现“XX万级首选/第一”这类排他表述）；' +
  '严禁把辅助驾驶表述为“自动驾驶/无人驾驶/完全自动”，统一用“组合驾驶辅助/辅助驾驶（L2）”并提示驾驶员需全程接管；' +
  '续航/油耗/加速/功率等数据须加“约/WLTC·CLTC工况/以官方公告为准”等限定；不编造数字，素材没有的信息用【待官方确认】或“待补充”，不要臆造。';

const BLOCK_FORMAT =
  '模块正文使用轻量标记（不要用 ``` 代码块）：\n' +
  '- 需要表格的模块：用 Markdown 表格，首行表头、第二行写 |---|---|，如\n' +
  '  | 要素 | 内容 |\n  | --- | --- |\n  | 做什么 | … |\n' +
  '- 模块内需要分层小标题时，用「### 小标题」单独成行（例如传播目标的三个维度、观点库的每个观点）；\n' +
  '- 并列要点用「- 」无序列表，需要排序时用「1. 」有序列表；其余为普通段落，每行不要过长。';

const GENERAL_MODULES: GenModuleSpec[] = [
  { key: 'background', title: '项目背景', guide: '品牌/车型背景、传播缘起、市场环境，2-3 句' },
  { key: 'objective', title: '传播目标', guide: '认知/态度/行为层面的具体目标，分条' },
  { key: 'audience', title: '目标人群', guide: '人群画像、用车场景、核心洞察，编号列出 3-4 类' },
  { key: 'sellingPoints', title: '核心卖点', guide: '3-5 条核心卖点，无序列表，参数需合规限定' },
  { key: 'keyMessage', title: '核心信息', guide: '主传播口号与信息屋（主信息+支撑点），口径合规' },
  { key: 'deliverables', title: '内容交付物', guide: '短视频/海报/KOL软文/新闻稿等形式与数量，可列表' },
  { key: 'channels', title: '传播渠道', guide: '媒体平台与渠道组合，可分重点/辐射' },
  { key: 'timeline', title: '时间排期', guide: '预热/爆发/长尾阶段与节点，素材缺失写待确认' },
  { key: 'budget', title: '预算范围', guide: '预算区间或分配建议，素材没有则写【待官方确认】' },
  { key: 'kpi', title: '效果评估', guide: '曝光/互动/线索等可量化指标，分条' },
  {
    key: 'notes',
    title: '备注与合规要求',
    kind: 'note',
    guide: '用“风险类别 + 审核要求”写法务/品牌口径（如：避免绝对化用语；数据须标注来源与统计口径；辅助驾驶不得描述为自动驾驶）。如确需引用被禁止的具体词做反面提醒，必须放在引号内并紧跟“不得/禁止/避免”，例如：不得使用“自动驾驶”表述。',
  },
];

const KOC_MODULES: GenModuleSpec[] = [
  {
    key: 'sixElements',
    title: '六要素参考',
    guide:
      '从素材中原话摘录，输出两列表格，表头「要素 | 内容」，固定 6 行：做什么 / 什么时候 / 在哪里 / 怎么做 / 重点 / 红线。' +
      '每行右栏只填素材中能对应到的原文信息；某行素材未提及，右栏照填「素材未提供」。不得为凑满表格而概括或编造。',
  },
  {
    key: 'infoSheet',
    title: '信息总表',
    guide:
      '从素材中原话摘录，输出两列表格，表头「项目 | 内容」，按素材已有信息列行（如传播车型、传播动作、传播定位、传播渠道、传播节点、核心Slogan/话题等）；' +
      '多个 #话题 在同一单元格内用空格分隔。素材没有的项目不要硬造行；如素材给出了固定项目但缺值，对应值填「素材未提供」。',
  },
  {
    key: 'commRules',
    title: '传播规范',
    guide:
      '从素材中原话摘录传播规范，必须包含四个子项，各占一个「### 」小标题：「### 传播基本要求」「### 剪辑」「### 画面」「### 口径」。' +
      '素材里属于这四类的要求（可含表格或列表）分别原话放入对应子项；某子项素材完全没有内容，就在该小标题下写「素材未提供」。不得自行补充行业通用建议。',
  },
  {
    key: 'objective',
    title: '传播目标',
    guide:
      '只把素材中明确写出的传播目标原话放入；若素材本身分了维度（如产品力/市场声量/用户心智），用「### 」按其原有维度分节，维度名沿用素材用词。' +
      '素材没有的维度不要补；整条目标素材都没写则填「素材未提供」。',
  },
  {
    key: 'audience',
    title: '目标受众',
    guide: '只把素材中写明的目标人群及其描述原话摘录，用有序或无序列表逐条列出，不新增人群画像、不扩写洞察；素材未写填「素材未提供」。',
  },
  {
    key: 'keyMessage',
    title: '传播内容',
    guide:
      '只摘录素材中要求传播的核心信息、Slogan、关键信息点，原话粘贴、完整列出，不重新组织结构、不润色、不提炼素材没有的信息屋；素材未写填「素材未提供」。',
  },
  {
    key: 'titleExamples',
    title: '标题实例',
    guide:
      '把素材中已经给出的标题/文案实例原样逐条罗列（有序列表），保留原文字句与顺序，不改写、不纠错、不新增任何标题；素材未给标题则填「素材未提供」。',
  },
  {
    key: 'viewpoints',
    title: '传播核心观点库',
    optional: true,
    guide:
      '【选填】仅当素材明确包含传播观点时才输出本模块，且观点数量、视角、结论与优先级全部以素材为准，原话摘录，不自行归纳或新增。' +
      '每个观点单独一行小标题，格式「### 观点N：【XX视角】+ 素材中的核心结论（空格）【★必选】」，结尾标签三选一【★必选】/【★推荐】/【可选】且与素材标注一致、只出现一次；' +
      '小标题下用「核心论点：」段落与「- 」列表如实摘录素材给出的论点/论据，若素材还给了该观点的示例标题，用「示例标题：」有序列出。素材没有观点时不要输出本模块。',
  },
  {
    key: 'productInfo',
    title: '产品信息附件',
    optional: true,
    guide:
      '【选填】仅当素材包含产品参数/配置/价格/权益/资料附件等信息时才输出本模块。用两列表格（表头「项目 | 内容」）或列表原样罗列素材给出的产品信息，数据与限定词（约/工况/以官方公告为准）一并保留；' +
      '不得补全或推算参数。素材完全没有这类信息时不要输出本模块。',
  },
];

const JOEY_MODULES: GenModuleSpec[] = [
  {
    key: 'sixElements',
    title: '六要素速览',
    guide:
      '从素材中原话摘录，输出两列表格，表头「要素 | 内容」，固定 6 行：做什么 / 什么时候 / 在哪里 / 怎么做 / 重点 / 红线。' +
      '右栏只填素材原文信息；某行素材未提及，右栏照填「素材未提供」。导购向素材中「重点」常含必选/推荐/可选观点，「红线」常含不点名竞品·不拉踩·无绝对化用语等，按原文填。',
  },
  {
    key: 'infoSheet',
    title: '信息总表',
    guide:
      '从素材中原话摘录，输出两列表格，表头「项目 | 内容」，按素材已有信息列行：传播车型（可含车型定性括注）、传播动作（如坐播口播脚本·60s 导购向）、传播定位、传播渠道（可含参考达人）、传播节点、核心标签词。' +
      '同一单元格多个标签用「 · 」分隔；素材没有的项目不硬造行，固定项目缺值填「素材未提供」。',
  },
  {
    key: 'commRules',
    title: '传播规范',
    guide:
      '从素材中原话摘录对内容生产与导购口径的规范，输出两列表格，表头「规范要点 | 要求」，每行一个要点（如价格诚意优先 / 导购为主体 / 级别差异不点名 / 引导线下 / 画面与封面），右栏放素材中的具体要求原文。' +
      '不补充素材没有的规范条目；完全没有则填「素材未提供」。',
  },
  {
    key: 'objective',
    title: '传播目标',
    guide:
      '只把素材中明确写出的传播目标原话放入，必须用三个固定「### 」小标题分节：「### 产品力维度」「### 市场声量维度」「### 用户心智维度」。' +
      '素材在某维度没有内容时，该小标题下写「素材未提供」；素材换用了维度名仍归一到这三节，不新增第四节。',
  },
  {
    key: 'audience',
    title: '目标受众',
    guide: '只把素材中写明的目标受众原话摘录，用「1. 2. 3.」有序列表逐条列出，不新增画像、不扩写洞察；素材未写填「素材未提供」。',
  },
  {
    key: 'assets',
    title: '官方固定资产与五大旗舰标准定性',
    guide:
      '从素材中原话摘录官方定性与资产：① 先用普通段落写明需高频绑定的品牌主张，并保留车名书写注意（如不可简写）；' +
      '② 用「### 五大旗舰级新标准」小标题 +「- 」列表列出标准（如豪华质感 / 三电系统 —— … / 驾控性能 —— … / 双智 —— … / 安全），原话保留参数与限定词；' +
      '③ 用「### 版型与价格（官方口径）」小标题 + 五列 Markdown 表格原样罗列全部版型，表头固定「版型 | 价格 | 续航 | 动力 | 关键差异」，一行一个版型，价格/续航/动力/差异均以素材为准，素材缺项填「素材未提供」，绝不推算或编造配置与价格。',
  },
  {
    key: 'viewpoints',
    title: '传播核心观点库与标题示例',
    optional: true,
    guide:
      '【选填】仅当素材明确包含观点时才输出本模块。观点数量、视角、结论、优先级与示例标题全部以素材为准，原话摘录，不自行归纳或新增。' +
      '每个观点单独一行小标题，格式「### 观点N【XX视角】+ 素材中的核心结论（空格）【★必选】」，结尾标签三选一【★必选】/【★推荐】/【可选】且与素材标注一致、只出现一次；' +
      '小标题下先写「核心论点：」段落（原话摘录），再换行写「示例标题：」并用「- 《标题》」无序列表原样罗列该观点给出的标题。素材没有观点/标题时不要输出本模块。',
  },
  {
    key: 'notes',
    title: '注意事项',
    kind: 'note',
    guide:
      '从素材中原话摘录合规与口径注意事项，用固定「### 」小标题组织，按素材实际有的内容填：「### 合规要求」（- 列表，如不点名竞品·统一泛指、不拉踩、无绝对化用语、辅助驾驶≠自动驾驶、数据与官方一致）、' +
      '「### 数据口径」（价格/配置/销量等以官方为准、特定数据仅在官方发布时使用）、「### 车型名称书写规范」（品牌/产品/完整名称/不可简写）、' +
      '「### 竞品对比（内部素材，脚本不点名）」（四列表格，表头「维度 | 本品 | 同级常见做法 | 脚本话术方向」，并在表后保留“脚本中不出现竞品名称、一律泛指”的说明）、' +
      '「### 传播信息」（两列表格：传播车型/主推版型/起售价/传播周期/传播渠道/内容形式）、「### 热销资产」（两列表格原样罗列热销与资产）。' +
      '素材没有的子项在该小标题下填「素材未提供」，不编造任何数据或竞品信息。',
  },
];

export const GEN_TEMPLATES: Record<
  GenTemplateId,
  { modules: GenModuleSpec[]; format: string; faithful?: boolean; titleSuffix?: string }
> = {
  general: { modules: GENERAL_MODULES, format: '模块正文用 Markdown：清单用无序列表，长段落不超过 3 句。' },
  koc: { modules: KOC_MODULES, format: BLOCK_FORMAT, faithful: true, titleSuffix: '传播 Brief' },
  joey: { modules: JOEY_MODULES, format: BLOCK_FORMAT, faithful: true, titleSuffix: '坐播传播 Brief' },
};

export function buildSystemPrompt(template: GenTemplateId): string {
  const spec = GEN_TEMPLATES[template] ?? GEN_TEMPLATES.general;
  const requiredKeys = spec.modules.filter((m) => !m.optional).map((m) => m.key);
  const optionalKeys = spec.modules.filter((m) => m.optional).map((m) => m.key);
  const keyRule = spec.faithful
    ? `必填模块（必须依次全部输出，即使无内容也要输出模块行并在正文写「素材未提供」）：${requiredKeys.join('、')}。\n` +
      `选填模块（仅当素材确有对应内容时才输出模块行与正文，否则整块省略，禁止为其编造或凑内容）：${optionalKeys.join('、')}。\n` +
      `除上述模块（含其下的「### 」固定小标题与表格）外，不要新增任何其他模块或独立章节。`
    : `必须且只能依次输出这些 key：${requiredKeys.join('、')}。每个 key 都要输出模块行；某模块素材不足时，正文给出合规占位（如【待官方确认】/待补充）而不是省略该模块。`;
  const titleSuffix = spec.titleSuffix ?? '传播 Brief';
  const titleRule = spec.faithful
    ? `在所有模块行之前，先单独输出一行标题标记：<<<TITLE:直接取素材中的车型/项目名称（原话），后接“ ${titleSuffix}”；素材无明确名称时用「未命名 ${titleSuffix}」，不要自行创作标题、不要书名号>>>，且整篇只出现一次。`
    : '在所有模块行之前，先单独输出一行标题标记：<<<TITLE:根据车型/项目拟定的 Brief 标题（10-24 字，不要书名号）>>>，且整篇只出现一次。';
  const faithfulBlock = spec.faithful ? `${FIDELITY}\n` : '';
  const complianceBlock = spec.faithful
    ? '合规要求：不主动编造素材中不存在的绝对化、排名、自动驾驶或数据表述；素材原文里的风险表述照实摘录即可，由系统另行检测，不要替用户改写。'
    : COMPLIANCE;
  return `你是资深汽车行业品牌传播策略专家，长期服务车企市场部与 KOC/KOL 内容代理，擅长把零散素材整理成结构清晰的传播 Brief。
${faithfulBlock}输出必须严格遵守：
0. ${titleRule}
1. 只输出“标题行 + 模块行 + 正文”。模块行格式为 <<<MODULE:key|模块名>>>，独占一行；正文紧随其后，直到下一个模块行。
2. ${keyRule}
3. ${spec.format}
4. ${spec.faithful ? '严格如实摘录，完整呈现素材给定内容，不额外补充、拓展、润色或修改原文。' : '内容专业、具体、可执行，忠实于素材，不添加素材之外的承诺与数据。'}
5. ${complianceBlock}
6. 不要输出任何解释、寒暄、代码块围栏或模块以外的内容。`;
}

export function buildModuleLines(template: GenTemplateId): string {
  const spec = GEN_TEMPLATES[template] ?? GEN_TEMPLATES.general;
  // 仅列出必填模块行作为骨架；选填模块（[选填]）只作提示，由模型按素材有无决定是否输出
  return spec.modules
    .map((m) =>
      m.optional
        ? `# [选填，素材确有内容才输出] <<<MODULE:${m.key}|${m.title}>>>`
        : `<<<MODULE:${m.key}|${m.title}>>>`,
    )
    .join('\n');
}

export function buildModuleGuides(template: GenTemplateId): string {
  const spec = GEN_TEMPLATES[template] ?? GEN_TEMPLATES.general;
  return spec.modules
    .map((m) => `- ${m.key}（${m.title}${m.optional ? '，选填' : ''}）：${m.guide}`)
    .join('\n');
}
